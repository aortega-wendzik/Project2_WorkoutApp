<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.7/handlebars.min.js"></script>
    <link rel="stylesheet" href="https://cdn.tailwindcss.com/2.2.17/tailwind.min.css" />
    <title>JANCS Workout App</title>
</head>

<body class="flex items-center justify-center min-h-screen bg-gray-200 py-4">
    <header class="mx-auto bg-white p-6 rounded shadow-md">
        <a href="#" class="text-blue-500">Sign Up</a>
    </header>
    <div class="container mx-auto py-8">
        <form id="loginForm" action="/login" method="POST" class="max-w-sm mx-auto bg-white p-6 rogit unded shadow-md">
            <h1 class="text-2xl text-center mb-6">WORKOUT WITH JANCS APP LOGIN</h1>
            <div class="mb-4">
                <input type="text" placeholder="Username" required
                    class="w-full px-4 py-2 border border-gray-300 rounded" />
            </div>
            <div class="flex items-center justify-between mb-4">
                <input type="password" placeholder="Password" required
                    class="w-full px-4 py-2 border border-gray-300 rounded" />
            </div>
            <div class="flex items-center justify-between mb-4">
                <label class="flex items-center">
                    <input type="checkbox" class="mr-2" />
                    <span>Remember me</span>
                </label>
                <a href="#" class="text-blue-500">Forgot password</a>
            </div>
            <button type="submit" class="w-full bg-blue-500 text-white py-2 px-4 rounded">Log In</button>
            <button type="reset" class="w-full bg-gray-300 text-gray-700 py-2 px-4 mt-2 rounded">Log Out</button>
        </form>
        <ul class="mt-4">
            {{#each items}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        {{#if isLoggedIn}}
        <p class="mt-4">Welcome, {{username}}!</p>
        {{else}}
        <p class="mt-4">Please Log In</p>
        {{/if}}
    </div>
</body>

</html>